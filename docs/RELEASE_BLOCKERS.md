# Items to resolve before public release

1. **Licence selected.** Owner-selected CC BY 4.0 is applied to author-owned material. LICENSE and NOTICE.md record its text and scope. Third-party terms remain separate.
2. **Dataset source/rights review.** The FLCHAIN/NWTCO cache mechanism was resolved by inspecting installed and upstream pycox code: the project reads raw caches, not pycox's default processed frames. An explicit acquisition script and observed cache hashes/row counts are now included. FLCHAIN=7,874 and NWTCO=4,028 are confirmed locally. A fresh provider download has not been compared with those historical hashes. The official DRSA repository identifies MUSIC as Last.fm-derived, contradicting the legacy loader comment calling it KKBox. Verify source provenance and time units with the authors; the original numerical code and manuscript were not changed.
3. **Simulation seed limitation.** E8/E7/E6c/E6d hash tuples containing strings; the original PYTHONHASHSEED was not recorded. Per-replication outputs and successful-replication counts are not consistently stored. Disclose the limitation; exact historical refitting is unavailable unless missing records are recovered. New hash-seeded runs use 0 and must be treated as new experiments.
4. **Correlation definition.** The original E8 algorithm uses `argsort(argsort(x))`, assigning distinct ranks to tied printed masses. It reproduces -0.1422 and +0.8742. Standard average-rank Spearman on the same printed 27 cells gives approximately -0.1469 and +0.8745. Resolve the naming/definition with the authors; changing to a tie-aware statistic would be a documented scientific revision, not a silent cleanup. No published number was changed here.
5. **Figure provenance.** The owner waived editable supplement source; the supplied PDF and labelled wrapper are retained. Original F1/F2/F3 plotting code was not supplied; numerical reconstructions are included for review.
6. **Software environment.** Observed version pins are provided, the offline environment passed a clean installation and full aggregate regeneration, but the model-refitting environment and complete data-based rerun have not been tested. Validate requirements-experiments.txt, especially the recorded CPU PyTorch version and pycox compatibility. The offline requirements installation is tested separately in this preparation. Do not label this environment fully validated until it is tested.
7. **Manuscript review.** All 34 S1/S2 rows and 25 S3/S4 paired metric differences can be checked against logs. Other numerical prose, theory claims and sensitivity statements need the per-line review in NUMBER_AUDIT.csv; the frozen verifier is not a proof audit. Original code docstrings/log commentary contain superseded scientific interpretations.
8. **Release metadata.** Author details are confirmed, and the repository URL and licence are filled. Actual release date and DOI will be added when known. Decisions are recorded in RELEASE_APPROVAL.json.

The owner created the GitHub repository and approved a public draft upload. No tag, GitHub release or Zenodo record has been created; these review items remain open.
